# 城市公共设施服务平台 - 项目详解

## 📋 目录

1. [项目概述](#项目概述)
2. [项目结构](#项目结构)
3. [前端详解](#前端详解)
4. [后端详解](#后端详解)
5. [数据库设计](#数据库设计)
6. [API 接口文档](#api-接口文档)
7. [部署说明](#部署说明)

---

## 🎯 项目概述

### 项目简介
基于 AI 视觉识别的城市公共设施报修与治理服务平台，提供用户报修、进度查询、AI 智能识别、管理员后台等功能。

### 技术栈

| 层级 | 技术选型 |
|------|----------|
| **前端** | HTML5 + CSS3 + 原生 JavaScript |
| **后端** | Hono.js (Cloudflare Workers) |
| **数据库** | Cloudflare D1 (SQLite) |
| **认证** | JWT (JSON Web Token) |
| **密码加密** | SHA-256 (Web Crypto API) |
| **部署** | Cloudflare Pages (前端) + Cloudflare Workers (后端) |

---

## 📁 项目结构

```
AI-city-public-service-platfom/
├── frontend/                    # 前端项目
│   ├── index.html              # 首页
│   ├── auth.html               # 登录/注册页
│   ├── repair.html             # 报修页
│   ├── query.html              # 进度查询页
│   ├── news.html               # 公告通知页
│   ├── admin.html              # 管理员后台
│   ├── ai-upload.html          # AI 智能报修页
│   ├── user-center.html        # 用户中心
│   ├── css/
│   │   └── style.css           # 全局样式
│   ├── js/
│   │   ├── api-client.js       # API 客户端
│   │   ├── main.js             # 主业务逻辑
│   │   ├── ai-api.js           # AI 相关功能
│   │   └── storage-manager.js  # 本地存储管理
│   └── images/                 # 图片资源
│       ├── Avatar/             # 用户头像
│       └── backgrounds/        # 背景图片
│
├── backend/                     # 后端项目
│   └── cloudflare-worker/
│       ├── src/
│       │   └── index.js        # 后端主文件
│       ├── schema.sql          # 数据库结构
│       ├── wrangler.toml       # Cloudflare 配置
│       ├── package.json        # 依赖配置
│       └── README.md
│
├── migrations/                  # 数据库迁移
│   └── 01_init_tables.sql
│
└── README.md                   # 项目说明
```

---

## 🎨 前端详解

### 1. 页面架构

#### 主要页面功能

| 页面 | 功能描述 |
|------|----------|
| **index.html** | 首页展示、功能导航、最新公告 |
| **auth.html** | 用户注册、用户登录、表单验证 |
| **repair.html** | 手动填写报修信息、上传图片 |
| **ai-upload.html** | AI 智能识别报修、图片上传与分析 |
| **query.html** | 查询报修进度、查看历史记录 |
| **news.html** | 查看公告通知、新闻资讯 |
| **user-center.html** | 用户信息管理、头像设置 |
| **admin.html** | 管理员后台、报修管理、用户管理 |

### 2. 核心模块详解

#### 2.1 api-client.js - API 客户端

**职责**：统一处理所有与后端的 API 通信

**核心功能**：

```javascript
// 1. API 基础配置
const API_BASE_URL = "https://xxx.workers.dev/api";

// 2. 请求锁机制 - 防止重复提交
const requestLocks = new Map();

// 3. 通用请求函数
async function apiRequest(url, options) {
  // 检查请求锁
  // 自动添加 Authorization header
  // 统一错误处理
  // 自动重定向登录
  // 释放请求锁
}

// 4. 用户相关 API
const api = {
  async register(name, phone, password) { /* ... */ },
  async login(phone, password) { /* ... */ },
  async getUserProfile() { /* ... */ },
  
  // 5. 报修相关 API
  async submitRepair(data) { /* ... */ },
  async getRepairs() { /* ... */ },
  async getRepairDetail(repairId) { /* ... */ },
  
  // 6. 管理员相关 API
  async adminLogin(username, password) { /* ... */ },
  async adminGetAllRepairs() { /* ... */ },
  async adminUpdateRepairStatus(repairId, status, progress) { /* ... */ },
};
```

**安全特性**：
- ✅ 请求锁防止重复提交
- ✅ 自动 Token 管理
- ✅ 401 自动跳转登录
- ✅ XSS 防护（escapeHtml）
- ✅ 输入验证（手机号、密码强度）

---

#### 2.2 main.js - 主业务逻辑

**职责**：页面初始化、事件绑定、UI 更新

**核心功能**：

```javascript
// 1. 用户状态管理
function checkAuth() {
  // 检查本地存储的 token
  // 更新用户区域显示
  // 未登录则显示登录按钮
}

// 2. 用户区域渲染
function renderUserArea(user) {
  // 显示用户头像和用户名
  // 添加下拉菜单（个人中心、退出登录）
}

// 3. 导航菜单
function initNavigation() {
  // 移动端菜单切换
  // 高亮当前页面
}

// 4. 页面功能初始化
document.addEventListener("DOMContentLoaded", () => {
  checkAuth();
  initNavigation();
  initPageSpecificFeatures();
});
```

---

#### 2.3 storage-manager.js - 本地存储管理

**职责**：封装 localStorage 操作

**功能**：
- Token 存储与读取
- 用户信息缓存
- 主题设置
- 搜索历史

---

### 3. 前端实现逻辑流程

#### 用户注册流程

```
用户填写表单
    ↓
前端验证（手机号格式、密码强度）
    ↓
调用 api.register()
    ↓
发送 POST /api/register
    ↓
后端验证（检查手机号是否已注册）
    ↓
密码加密存储
    ↓
返回成功响应
    ↓
前端跳转到登录页
```

#### 用户登录流程

```
用户输入手机号和密码
    ↓
前端验证
    ↓
调用 api.login()
    ↓
发送 POST /api/login
    ↓
后端查询用户
    ↓
验证密码（SHA-256）
    ↓
生成 JWT Token
    ↓
返回 Token 和用户信息
    ↓
前端存储 Token 到 localStorage
    ↓
更新 UI 显示用户信息
    ↓
跳转到首页
```

#### 报修提交流程

```
用户填写报修信息
    ↓
上传图片（可选）
    ↓
前端验证必填字段
    ↓
调用 api.submitRepair()
    ↓
发送 POST /api/repairs（带 Authorization header）
    ↓
后端验证 Token
    ↓
生成唯一报修 ID
    ↓
保存到数据库
    ↓
返回成功
    ↓
前端显示成功提示
    ↓
跳转到进度查询页
```

---

## ⚙️ 后端详解

### 1. 技术架构

#### 后端技术栈

| 组件 | 说明 |
|------|------|
| **Hono.js** | 轻量级 Web 框架，专为 Edge 环境设计 |
| **Cloudflare Workers** | 无服务器计算平台，全球边缘节点 |
| **Cloudflare D1** | 基于 SQLite 的边缘数据库 |
| **Web Crypto API** | 原生加密 API，替代 bcryptjs 和 jsonwebtoken |

---

### 2. 核心模块详解

#### 2.1 密码加密模块

使用 Web Crypto API 替代 Node.js 专用库，兼容 Cloudflare Workers 环境：

```javascript
// SHA-256 密码哈希
async function hashPassword(password) {
  const encoder = new TextEncoder();
  const data = encoder.encode(password + JWT_SECRET);
  const hashBuffer = await crypto.subtle.digest("SHA-256", data);
  const hashArray = Array.from(new Uint8Array(hashBuffer));
  return hashArray.map(b => b.toString(16).padStart(2, "0")).join("");
}

// 密码验证
async function verifyPassword(password, hashedPassword) {
  const hash = await hashPassword(password);
  return hash === hashedPassword;
}
```

**为什么选择 SHA-256？**
- ✅ Cloudflare Workers 原生支持
- ✅ 无需额外依赖
- ✅ 计算速度快
- ⚠️ 注意：生产环境建议使用加盐（salt）

---

#### 2.2 JWT 认证模块

使用 Web Crypto API 实现 JWT 生成和验证：

```javascript
// 生成 JWT Token
async function generateToken(userId) {
  const header = { alg: "HS256", typ: "JWT" };
  const payload = { userId, exp: Date.now() + 24 * 60 * 60 * 1000 };
  
  // 1. Base64 编码 Header 和 Payload
  const headerB64 = btoa(JSON.stringify(header));
  const payloadB64 = btoa(JSON.stringify(payload));
  
  // 2. 创建签名
  const data = `${headerB64}.${payloadB64}`;
  const key = await crypto.subtle.importKey(
    "raw",
    encoder.encode(JWT_SECRET),
    { name: "HMAC", hash: "SHA-256" },
    false,
    ["sign"]
  );
  const signature = await crypto.subtle.sign("HMAC", key, data);
  
  // 3. 组合 Token
  return `${headerB64}.${payloadB64}.${signatureB64}`;
}

// 验证 JWT Token
async function verifyToken(token) {
  // 1. 拆分 Token
  const [headerB64, payloadB64, sigB64] = token.split(".");
  
  // 2. 验证签名
  const isValid = await crypto.subtle.verify(/* ... */);
  if (!isValid) return null;
  
  // 3. 检查过期时间
  const payload = JSON.parse(atob(payloadB64));
  if (payload.exp < Date.now()) return null;
  
  return payload;
}
```

**JWT 结构说明**：
```
Header.Payload.Signature
  ↓      ↓        ↓
 算法  用户ID  HMAC签名
 类型  过期时间
```

---

#### 2.3 CORS 中间件

```javascript
import { cors } from "hono/cors";

app.use("/*", cors({
  origin: "*",              // 允许所有来源（生产环境建议限制）
  credentials: true,        // 允许携带凭证
}));
```

---

### 3. API 路由设计

#### 路由顺序原则

```javascript
// 1. 无认证接口放在最前面（按使用频率排序）
app.get("/api/health", ...);        // 健康检查
app.get("/", ...);                   // 根路径
app.post("/api/register", ...);      // 注册
app.post("/api/login", ...);         // 登录
app.get("/api/news", ...);           // 新闻列表

// 2. 需要认证的用户接口
app.get("/api/user/profile", ...);   // 用户信息
app.post("/api/repairs", ...);       // 提交报修
app.get("/api/repairs", ...);        // 我的报修

// 3. 管理员接口（需要认证 + 权限检查）
app.post("/api/admin/login", ...);
app.get("/api/admin/repairs", ...);
app.put("/api/admin/repairs/:id", ...);
```

---

### 4. 后端实现逻辑流程

#### 注册接口实现流程

```javascript
app.post("/api/register", async (c) => {
  // 1. 获取数据库连接
  const db = c.env.DB;
  
  // 2. 解析请求体
  const body = await c.req.json();
  const { name, phone, password } = body;
  
  // 3. 输入验证
  if (!name || name.length < 2)
    return c.json({ success: false, message: "..." }, 422);
  if (!validatePhone(phone))
    return c.json({ success: false, message: "..." }, 422);
  if (!validatePassword(password))
    return c.json({ success: false, message: "..." }, 422);
  
  // 4. 检查手机号是否已注册
  const existingUser = await db
    .prepare("SELECT id FROM users WHERE phone = ?")
    .bind(phone)
    .first();
  if (existingUser)
    return c.json({ success: false, message: "..." }, 409);
  
  // 5. 加密密码
  const hashedPassword = await hashPassword(password);
  
  // 6. 插入数据库
  await db
    .prepare("INSERT INTO users (name, phone, password) VALUES (?, ?, ?)")
    .bind(sanitizeInput(name), phone, hashedPassword)
    .run();
  
  // 7. 返回成功
  return c.json({ success: true, message: "注册成功" });
});
```

---

#### 登录接口实现流程

```javascript
app.post("/api/login", async (c) => {
  const db = c.env.DB;
  const { phone, password } = await c.req.json();
  
  // 1. 查询用户
  const user = await db
    .prepare("SELECT * FROM users WHERE phone = ?")
    .bind(phone)
    .first();
  if (!user)
    return c.json({ success: false, message: "..." }, 401);
  
  // 2. 验证密码
  const isValid = await verifyPassword(password, user.password);
  if (!isValid)
    return c.json({ success: false, message: "..." }, 401);
  
  // 3. 生成 JWT Token
  const token = await generateToken(user.id);
  
  // 4. 返回 Token 和用户信息（不包含密码）
  return c.json({
    success: true,
    token,
    user: {
      id: user.id,
      name: user.name,
      phone: user.phone,
    },
  });
});
```

---

#### 提交报修接口实现流程

```javascript
app.post("/api/repairs", async (c) => {
  // 1. 验证 Token
  const authHeader = c.req.header("Authorization");
  if (!authHeader?.startsWith("Bearer "))
    return c.json({ success: false, message: "未授权" }, 401);
  
  const token = authHeader.split(" ")[1];
  const decoded = await verifyToken(token);
  if (!decoded)
    return c.json({ success: false, message: "Token 无效" }, 401);
  
  // 2. 获取报修数据
  const body = await c.req.json();
  const db = c.env.DB;
  
  // 3. 生成唯一报修 ID
  function generateRepairId() {
    const timestamp = Date.now().toString(36).toUpperCase();
    const random = Math.random().toString(36).substring(2, 6).toUpperCase();
    return `R${timestamp}${random}`; // 例如: RKX8Y3Z1A2
  }
  
  // 4. 插入数据库
  await db
    .prepare(`INSERT INTO repairs 
      (repair_id, user_id, facility_type, damage_type, 
       location, description, image, status, progress)
      VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)`)
    .bind(
      generateRepairId(),
      decoded.userId,
      body.facility_type,
      body.damage_type,
      sanitizeInput(body.location),
      sanitizeInput(body.description),
      body.image,
      "pending",   // 初始状态：待处理
      0             // 初始进度：0%
    )
    .run();
  
  return c.json({ success: true, message: "报修提交成功" });
});
```

---

#### 管理员更新报修状态流程

```javascript
app.put("/api/admin/repairs/:id", async (c) => {
  // 1. 验证 Token
  const decoded = await verifyTokenFromHeader(c);
  if (!decoded) return c.json({ ... }, 401);
  
  // 2. 检查是否为管理员
  const db = c.env.DB;
  const admin = await db
    .prepare("SELECT id FROM admins WHERE id = ?")
    .bind(decoded.userId)
    .first();
  if (!admin) return c.json({ ... }, 403); // 403 Forbidden
  
  // 3. 获取参数
  const repairId = c.req.param("id");
  const { status, progress, admin_note } = await c.req.json();
  
  // 4. 验证状态值
  const validStatuses = ["pending", "processing", "completed", "cancelled"];
  if (status && !validStatuses.includes(status))
    return c.json({ ... }, 422);
  if (progress !== undefined && (progress < 0 || progress > 100))
    return c.json({ ... }, 422);
  
  // 5. 更新数据库
  await db
    .prepare(`UPDATE repairs 
      SET status = ?, progress = ?, admin_note = ?, update_time = CURRENT_TIMESTAMP
      WHERE repair_id = ?`)
    .bind(status, progress, sanitizeInput(admin_note), repairId)
    .run();
  
  return c.json({ success: true, message: "更新成功" });
});
```

---

## 🗄️ 数据库设计

### 数据库表结构

#### 1. users 表 - 用户表

| 字段 | 类型 | 说明 | 约束 |
|------|------|------|------|
| id | INTEGER | 用户 ID | PRIMARY KEY, AUTOINCREMENT |
| name | TEXT | 用户姓名 | NOT NULL |
| phone | TEXT | 手机号 | UNIQUE, NOT NULL |
| password | TEXT | 密码（加密） | NOT NULL |
| avatar | TEXT | 头像 URL | NULLABLE |
| created_at | DATETIME | 创建时间 | DEFAULT CURRENT_TIMESTAMP |

**索引**：
- `idx_users_phone` - 手机号索引（加速登录查询）

---

#### 2. repairs 表 - 报修记录表

| 字段 | 类型 | 说明 | 约束 |
|------|------|------|------|
| id | INTEGER | 自增 ID | PRIMARY KEY |
| repair_id | TEXT | 报修编号 | UNIQUE, NOT NULL |
| user_id | INTEGER | 用户 ID | FOREIGN KEY → users.id |
| facility_type | TEXT | 设施类型 | NULLABLE |
| damage_type | TEXT | 损坏类型 | NULLABLE |
| location | TEXT | 位置描述 | NULLABLE |
| description | TEXT | 详细描述 | NULLABLE |
| image | TEXT | 图片 URL | NULLABLE |
| status | TEXT | 状态 | DEFAULT 'pending' |
| progress | INTEGER | 进度(0-100) | DEFAULT 0, CHECK 0-100 |
| admin_note | TEXT | 管理员备注 | NULLABLE |
| submit_time | DATETIME | 提交时间 | DEFAULT CURRENT_TIMESTAMP |
| update_time | DATETIME | 更新时间 | DEFAULT CURRENT_TIMESTAMP |

**状态枚举**：
- `pending` - 待处理
- `processing` - 处理中
- `completed` - 已完成
- `cancelled` - 已取消

**索引**：
- `idx_repairs_user_id` - 用户 ID 索引
- `idx_repairs_repair_id` - 报修编号索引
- `idx_repairs_status` - 状态索引
- `idx_repairs_submit_time` - 提交时间索引（降序）

---

#### 3. admins 表 - 管理员表

| 字段 | 类型 | 说明 | 约束 |
|------|------|------|------|
| id | INTEGER | 管理员 ID | PRIMARY KEY |
| username | TEXT | 用户名 | UNIQUE, NOT NULL |
| password | TEXT | 密码 | NOT NULL |
| name | TEXT | 姓名 | NULLABLE |
| created_at | DATETIME | 创建时间 | DEFAULT CURRENT_TIMESTAMP |

**默认管理员**：
- 用户名：`admin`
- 密码：`admin123`

---

#### 4. notifications 表 - 通知表

| 字段 | 类型 | 说明 | 约束 |
|------|------|------|------|
| id | INTEGER | 通知 ID | PRIMARY KEY |
| user_id | INTEGER | 用户 ID | FOREIGN KEY → users.id |
| title | TEXT | 标题 | NOT NULL |
| content | TEXT | 内容 | NULLABLE |
| is_read | INTEGER | 是否已读 | DEFAULT 0, CHECK 0/1 |
| created_at | DATETIME | 创建时间 | DEFAULT CURRENT_TIMESTAMP |

**索引**：
- `idx_notifications_user_id` - 用户 ID 索引
- `idx_notifications_is_read` - 已读状态索引

---

#### 5. news 表 - 新闻表

| 字段 | 类型 | 说明 | 约束 |
|------|------|------|------|
| id | INTEGER | 新闻 ID | PRIMARY KEY |
| title | TEXT | 标题 | NOT NULL |
| content | TEXT | 内容 | NULLABLE |
| publish_date | DATE | 发布日期 | DEFAULT CURRENT_DATE |

---

### 数据库关系图

```
users (用户表)
  ↓ (1)
  ↓
  ↓ (N)
repairs (报修表) ←←← admins (管理员表)
  ↓
  ↓ (1)
  ↓
  ↓ (N)
notifications (通知表)

news (新闻表) - 独立表
```

---

## 🔌 API 接口文档

### 基础信息

- **Base URL**: `https://city-public-service-platfom.xxx.workers.dev/api`
- **认证方式**: Bearer Token (Authorization header)
- **数据格式**: JSON

---

### 公共接口

#### 1. 健康检查

```http
GET /api/health
```

**响应示例**：
```json
{
  "success": true,
  "message": "服务正常运行",
  "timestamp": "2026-03-21T13:00:00.000Z"
}
```

---

#### 2. 用户注册

```http
POST /api/register
Content-Type: application/json

{
  "name": "张三",
  "phone": "13800138000",
  "password": "123456"
}
```

**响应**：
```json
{
  "success": true,
  "message": "注册成功"
}
```

**错误码**：
- `422` - 参数错误（手机号格式、密码强度）
- `409` - 手机号已注册

---

#### 3. 用户登录

```http
POST /api/login
Content-Type: application/json

{
  "phone": "13800138000",
  "password": "123456"
}
```

**响应**：
```json
{
  "success": true,
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "id": 1,
    "name": "张三",
    "phone": "13800138000"
  }
}
```

---

#### 4. 新闻列表

```http
GET /api/news
```

**响应**：
```json
{
  "success": true,
  "news": [
    {
      "id": 1,
      "title": "平台正式上线",
      "content": "...",
      "publish_date": "2026-03-21"
    }
  ]
}
```

---

### 用户接口（需要认证）

#### 5. 获取用户信息

```http
GET /api/user/profile
Authorization: Bearer <token>
```

**响应**：
```json
{
  "success": true,
  "user": {
    "id": 1,
    "name": "张三",
    "phone": "13800138000",
    "created_at": "2026-03-21T10:00:00.000Z"
  }
}
```

---

#### 6. 提交报修

```http
POST /api/repairs
Authorization: Bearer <token>
Content-Type: application/json

{
  "facility_type": "路灯",
  "damage_type": "不亮",
  "location": "XX路与YY路交叉口",
  "description": "路灯从昨天开始不亮了",
  "image": "https://..."
}
```

**响应**：
```json
{
  "success": true,
  "message": "报修提交成功"
}
```

---

#### 7. 获取我的报修列表

```http
GET /api/repairs
Authorization: Bearer <token>
```

**响应**：
```json
{
  "success": true,
  "repairs": [
    {
      "repair_id": "RKX8Y3Z1A2",
      "facility_type": "路灯",
      "status": "processing",
      "progress": 50,
      "submit_time": "2026-03-21T10:00:00.000Z"
    }
  ]
}
```

---

#### 8. 获取报修详情

```http
GET /api/repairs/:repair_id
Authorization: Bearer <token>
```

---

#### 9. 删除报修

```http
DELETE /api/repairs/:repair_id
Authorization: Bearer <token>
```

---

#### 10. 通知相关

```http
GET /api/notifications
GET /api/notifications/unread-count
PUT /api/notifications/:id/read
```

---

### 管理员接口

#### 11. 管理员登录

```http
POST /api/admin/login
Content-Type: application/json

{
  "username": "admin",
  "password": "admin123"
}
```

**响应**：
```json
{
  "success": true,
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "id": 1,
    "username": "admin",
    "name": "管理员",
    "role": "admin"
  }
}
```

---

#### 12. 获取所有报修

```http
GET /api/admin/repairs
Authorization: Bearer <admin-token>
```

**响应**：
```json
{
  "success": true,
  "repairs": [
    {
      "repair_id": "RKX8Y3Z1A2",
      "user_name": "张三",
      "user_phone": "13800138000",
      "facility_type": "路灯",
      "status": "pending",
      ...
    }
  ]
}
```

---

#### 13. 更新报修状态

```http
PUT /api/admin/repairs/:repair_id
Authorization: Bearer <admin-token>
Content-Type: application/json

{
  "status": "processing",
  "progress": 50,
  "admin_note": "已安排维修人员"
}
```

---

#### 14. 用户管理

```http
GET /api/admin/users
DELETE /api/admin/users/:id
```

---

#### 15. 新闻管理

```http
POST /api/admin/news
DELETE /api/admin/news/:id
```

---

#### 16. 统计数据

```http
GET /api/admin/stats
Authorization: Bearer <admin-token>
```

**响应**：
```json
{
  "success": true,
  "stats": {
    "totalRepairs": 100,
    "completedRepairs": 80,
    "pendingRepairs": 10,
    "processingRepairs": 10,
    "totalUsers": 50,
    "completionRate": 80
  }
}
```

---

## 🚀 部署说明

### 前端部署 (Cloudflare Pages)

1. 上传 `frontend/` 目录到 Cloudflare Pages
2. 配置构建命令：无需构建（纯静态）
3. 配置输出目录：`frontend/`
4. 部署完成后获得访问 URL

---

### 后端部署 (Cloudflare Workers)

#### 前置要求
- Node.js 18+
- Cloudflare 账户

#### 部署步骤

```bash
# 1. 进入后端目录
cd backend/cloudflare-worker

# 2. 安装依赖
npm install

# 3. 登录 Cloudflare
npx wrangler login

# 4. 创建 D1 数据库
npx wrangler d1 create city-service-db

# 5. 执行数据库初始化
npx wrangler d1 execute city-service-db --file=./schema.sql

# 6. 更新 wrangler.toml 中的 database_id

# 7. 部署
npx wrangler deploy
```

---

### 本地开发

#### 前端本地运行

```bash
cd frontend
python -m http.server 8080
# 或使用其他静态服务器
```

访问：http://localhost:8080

---

#### 后端本地运行

```bash
cd backend/cloudflare-worker
npm install
npx wrangler dev
```

访问：http://localhost:8787

---

## 🔐 安全特性

### 已实现的安全措施

1. ✅ **密码加密** - SHA-256 哈希
2. ✅ **JWT 认证** - Token 有效期 24 小时
3. ✅ **SQL 注入防护** - 使用参数化查询
4. ✅ **XSS 防护** - 输入 sanitize
5. ✅ **CORS 配置** - 跨域控制
6. ✅ **请求锁** - 防止重复提交
7. ✅ **输入验证** - 手机号、密码强度
8. ✅ **权限分离** - 用户/管理员接口分离

---

## 📝 总结

### 前端特点
- 🎨 纯原生 HTML/CSS/JS，无框架依赖
- 📱 响应式设计
- 🔌 统一的 API 客户端封装
- 🛡️ 完善的错误处理

### 后端特点
- ⚡ Hono.js 轻量级框架
- 🌍 Cloudflare Workers 全球边缘部署
- 💾 Cloudflare D1 SQLite 数据库
- 🔐 Web Crypto API 原生加密
- 📊 完整的 CRUD 接口

### 数据库特点
- 🗃️ 5 张核心表
- 🔗 外键级联删除
- 📈 索引优化查询性能
- 🎯 默认数据初始化

---

**文档版本**: 1.0  
**最后更新**: 2026-03-21

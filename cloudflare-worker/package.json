{
  "name": "city-service-backend",
  "version": "1.0.0",
  "description": "Cloudflare Worker backend for city service platform",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute city-service-db --file=./schema.sql",
    "db:create": "wrangler d1 create city-service-db"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
